<% if @collection.errors.any? %>
    
      The form contains <%= pluralize(@collection.errors.count, "error") %>.
    
    <ul>
    <% @collection.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  
<% end %>